import { ShieldCheck } from "lucide-react";
import { useAuth } from "@/hooks/useAuth";
import { useOrganizationContext } from "@/hooks/useOrganizationContext";
export function VerifiedBadge(){const auth=useAuth(),organization=useOrganizationContext(auth.isAuthenticated);const current=organization.context?.state==="available"?organization.context.organization:null;if(current?.verification.canonicalTrustStatus!=="trusted")return null;return <div className="flex w-fit items-center gap-3 rounded-2xl border bg-white/80 p-3 shadow-md"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-emerald-600"><ShieldCheck className="h-5 w-5 text-white"/></div><div><div className="text-sm font-semibold text-emerald-700">Organization trusted</div><div className="text-xs text-gray-600">Canonical verification status</div></div></div>}
