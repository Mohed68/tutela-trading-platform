import { useQuery } from "@tanstack/react-query";
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";
import { getQueryFn } from "@/lib/queryClient";
type Contract={contractId:string;orderId:string;offerId:string;status:"draft";contractVersion:number;createdAt:string;contractFingerprint:string;terms:{quantity:string;unit:string;pricePerUnit:string;totalAmount:string;currency:string;deliveryTerms:string|null;paymentTerms:string|null;specifications:string|null}};
export default function Contracts(){const{data=[],isLoading}=useQuery<Contract[]>({queryKey:["/api/contracts"],queryFn:getQueryFn({on401:"throw"})});return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Contracts</h1><p className="text-sm text-neutral-600">Immutable Current V2 draft snapshots created from accepted orders. Signing, payment, settlement, and shipping are not active capabilities.</p></div>{isLoading?<p>Loading contracts…</p>:data.length===0?<Card><CardContent className="py-8 text-sm text-neutral-600">No V2 contract drafts yet. A buyer or seller may create one after the seller accepts an order.</CardContent></Card>:<div className="grid gap-4">{data.map(contract=><Card key={contract.contractId}><CardHeader><CardTitle className="text-lg">Contract draft · {contract.terms.quantity} {contract.terms.unit}</CardTitle></CardHeader><CardContent className="space-y-2 text-sm"><p>Status: <strong>draft</strong> · version {contract.contractVersion}</p><p>{contract.terms.totalAmount} {contract.terms.currency} · {contract.terms.pricePerUnit} per {contract.terms.unit}</p>{contract.terms.deliveryTerms&&<p>Delivery terms: {contract.terms.deliveryTerms}</p>}{contract.terms.paymentTerms&&<p>Payment terms recorded from offer: {contract.terms.paymentTerms}</p>}<p className="break-all text-xs text-neutral-500">Contract {contract.contractId} · Order {contract.orderId}</p></CardContent></Card>)}</div>}</div>}
